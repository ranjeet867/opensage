# Install OpenSage

The full install guide lives in [README.md](README.md). The short version:

1. Clone this repo.
2. `chrome://extensions` → enable Developer mode → Load unpacked → pick this folder.
3. Click the OpenSage icon → paste your Anthropic API key → Save.

That's it. See [README.md](README.md) for everything else.
